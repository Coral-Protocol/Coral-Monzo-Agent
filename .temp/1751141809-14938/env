#!/bin/sh
# add binaries to PATH if they aren't added yet
# affix colons on either side of $PATH to simplify matching
case ":${PATH}:" in
    *:"/home/xinxing/coral/coral-orchestrator/Coral-Monzo-Agent/.temp/1751141809-14938":*)
        ;;
    *)
        # Prepending path in case a system-installed binary needs to be overridden
        export PATH="/home/xinxing/coral/coral-orchestrator/Coral-Monzo-Agent/.temp/1751141809-14938:$PATH"
        ;;
esac
